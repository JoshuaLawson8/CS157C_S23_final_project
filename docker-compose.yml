#This describes the different sections of the project

version: '3.8'

services:

  frontend:
    build: ./frontend
    hostname: frontend
    container_name: frontend
    stdin_open: true
    ports:
      - "3000:3000"

  backend:
    build: ./backend
    hostname: backend
    container_name: backend
